# Platform SDK 升级清单

状态：执行前
当前阶段：S0 契约冻结（未开始）
完成阶段：0 / 7

本清单只记录平台 SDK 的阶段任务、出口和完成证据。系统职责、依赖方向、状态所有权和
生命周期以 `ARCHITECTURE.md` 为准；线上字段和事件以服务端正式契约及 `protocol/` 为准。

## 标记规则

- `[ ]` 未开始
- `[~]` 进行中
- `[x]` 已完成
- `[!]` 阻塞，必须在备注中记录原因和解除条件

每个阶段必须同时记录状态、完成日期、提交、验证命令或测试结果和遗留风险。只有出口清单
全部完成并有证据时，阶段标题才能改为 `[x]`；未完成项不得通过勾选父项掩盖。

## 总进度

- [ ] S0 契约冻结
- [ ] S1 公共 API
- [ ] S2 远程适配
- [ ] S3 本地适配
- [ ] S4 能力扩展
- [ ] S5 前端迁移
- [ ] S6 发布兼容

## [ ] S0 契约冻结

### 任务

- [ ] 确定 SDK 首发语言、同步/异步模型和支持的平台范围。
- [ ] 列出 Client、Session、Turn、Event、Tool、Approval、Effect、错误和结果的公开对象。
- [ ] 冻结 identity、可空性、关闭语义、错误分类和事件游标规则。
- [ ] 定义版本策略、兼容级别和不兼容变更处理方式。
- [ ] 标出 `agent`、`protocol`、`infrastructure` 和第三方对象中禁止外泄的实现细节。
- [ ] 形成 SDK 公开入口与内部适配层的依赖图。

### 出口

- [ ] 公开对象清单经架构评审。
- [ ] 依赖图确认 SDK 不被内部包反向依赖。
- [ ] 版本和兼容策略可以指导后续 API 评审。
- [ ] 没有重复状态模型、无归属 facade 或隐式全局依赖。

### 记录

- 状态：未开始
- 完成日期：
- 提交：
- 验证：
- 遗留风险/决策：

## [ ] S1 公共 API

### 任务

- [ ] 建立非 UI 的 `sdk/` 公共包。
- [ ] 实现最小不可变模型、命令、事件、结果和错误类型。
- [ ] 提供显式 Client、Session、Turn 生命周期和关闭契约。
- [ ] 通过受控 `__all__` 暴露公共对象，隐藏 Store、Actor、Adapter 和第三方类型。
- [ ] 添加 SDK 独立导入和最小会话观察示例。

### 出口

- [ ] SDK 可在没有 TUI、CLI 或具体 infrastructure 的环境中导入。
- [ ] 公开导出快照和类型契约测试通过。
- [ ] SDK 包不反向导入 `agent`、`infrastructure` 或 `frontends`。
- [ ] 最小示例不需要访问内部模块。

### 记录

- 状态：未开始
- 完成日期：
- 提交：
- 验证：
- 遗留风险/决策：

## [ ] S2 远程适配

### 任务

- [ ] 将 `protocol/client` 封装为 SDK 远程实现。
- [ ] 统一 attach、replay、事件游标和传输重试语义。
- [ ] 统一审批快照、工具结果和稳定错误映射。
- [ ] 隔离 HTTP、SSE 和第三方载荷，不使其进入公共模型。

### 出口

- [ ] 断线恢复、重复事件、序号缺口和 `turn.logical_settled` 有契约测试。
- [ ] 审批恢复和工具结果投递保持幂等。
- [ ] 远程 SDK 只返回统一事件、结果和错误。

### 记录

- 状态：未开始
- 完成日期：
- 提交：
- 验证：
- 遗留风险/决策：

## [ ] S3 本地适配

### 任务

- [ ] 将 `agent.application` 用例映射到 SDK Client、Session 和 Turn 契约。
- [ ] 将 `agent.ports` 能力转换为显式 SDK Provider，不暴露内部端口集合。
- [ ] 由组合根注入本地实现、配置和资源关闭顺序。
- [ ] 确保 SDK 不读取配置文件、不创建 UI、不动态发现本地服务。

### 出口

- [ ] 同一 SDK 用例可以切换远程和本地实现。
- [ ] Session 单写者、取消、关闭、恢复和终态语义一致。
- [ ] 本地适配没有重复状态机、隐式 fallback 或临时 facade。

### 记录

- 状态：未开始
- 完成日期：
- 提交：
- 验证：
- 遗留风险/决策：

## [ ] S4 能力扩展

### 任务

- [ ] 定义 Tool、Approval、Effect、Attachment、`exec_env`、Hook 和 Sidecar 的 SDK 扩展点。
- [ ] 使用具名 Provider 或注册契约注入能力，不暴露具体实现对象。
- [ ] 在边界归一化工具结果和错误。
- [ ] 将未知外部效果接入既有 reconciliation 路径。
- [ ] 确认权限、内部日志、效果账本和供应商载荷不会进入公共模型。

### 出口

- [ ] 客户端工具、托管工具、审批和 Sidecar 均可通过 typed contract 使用。
- [ ] 权限和效果状态在本地与远程实现中保持一致。
- [ ] 关键失败路径、取消和未知效果均有测试证据。

### 记录

- 状态：未开始
- 完成日期：
- 提交：
- 验证：
- 遗留风险/决策：

## [ ] S5 前端迁移

### 任务

- [ ] 迁移 TUI，使其只依赖 SDK 公共入口和展示投影。
- [ ] 迁移 CLI、Subscription 和 stdio MCP，删除内部模块直连。
- [ ] 为桌面端和 Web 提供 SDK 适配示例或参考实现。
- [ ] 删除前端对 `agent.ports`、`protocol` transport、组合对象和具体 infrastructure 的直接依赖。
- [ ] 统一 retry/presentation 替代、审批快照、事件去重和结算展示。

### 出口

- [ ] 至少两种不同前端使用同一 SDK 命令和事件模型。
- [ ] 同一 Turn 在不同前端的状态、错误和结算展示一致。
- [ ] 旧导入路径和同义 facade 已删除，或记录仍需保留的外部契约。

### 记录

- 状态：未开始
- 完成日期：
- 提交：
- 验证：
- 遗留风险/决策：

## [ ] S6 发布兼容

### 任务

- [ ] 定义 SDK 包元数据、版本矩阵和变更记录。
- [ ] 配置源码分发、wheel 和平台包只包含声明的 SDK 公共模块。
- [ ] 验证 npm、桌面和 Web 适配器不复制 Python 内部实现。
- [ ] 建立干净环境安装、导入、升级和回滚检查。
- [ ] 校验发布清单、资产散列、依赖方向和公共 API 稳定性。

### 出口

- [ ] 干净环境可以安装并导入 SDK。
- [ ] 版本升级、旧客户端、缺少可选能力和关闭失败都有确定结果。
- [ ] 发布产物与源码结构、公开 API 和版本矩阵一致。

### 记录

- 状态：未开始
- 完成日期：
- 提交：
- 验证：
- 遗留风险/决策：

## [ ] 最终验收

- [ ] TUI、桌面/Web 和至少一个远程或本地宿主复用同一 SDK 契约。
- [ ] 命令、事件、游标、工具结果、审批和效果对账在不同实现间语义一致。
- [ ] 前端不直接导入内部 Harness、Transport、Store、第三方 SDK 或配置路径。
- [ ] 公共 API、版本策略、错误分类和关闭顺序均有契约测试。
- [ ] 架构边界审计、受影响行为测试、语法检查和发布产物验证全部通过。

验收完成后，将总进度和对应阶段记录改为 `[x]`，并在最后一项记录最终提交和验证结果。
