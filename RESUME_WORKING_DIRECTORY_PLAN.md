# 恢复会话工作目录对齐实施清单

本清单限定于 CLI/TUI 的历史会话恢复、目录选择及其必要的工作区生命周期。
架构以 `ARCHITECTURE.md`、`ARCHITECTURE_SYSTEM.md` 为准。会话继续使用本项目的
`cid/sid`、本地索引保留期限和正式协议；不引入 Codex 的远端线程协议或修改 Helix Authority。

## 对齐基准

- [目录策略、历史目录读取、缺失元数据处理](codex-main/codex-rs/tui/src/session_resume.rs)
- [四项菜单、记忆策略、Esc 与 Ctrl+C 行为](codex-main/codex-rs/tui/src/cwd_prompt.rs)
- [启动恢复、指定 ID 查询、--all 过滤](codex-main/codex-rs/tui/src/lib.rs)
- [应用内恢复与启动目录锚点](codex-main/codex-rs/tui/src/app/session_lifecycle.rs)
- [按目标工作目录重建配置](codex-main/codex-rs/tui/src/app/config_persistence.rs)
- [路径身份比较](codex-main/codex-rs/utils/path-utils/src/lib.rs)
- [恢复策略配置](codex-main/codex-rs/config/src/types.rs)
- [恢复菜单及目录行为测试](codex-main/codex-rs/tui/src/app/tests.rs)

固定验收语义：显式 `-C/--cd` 优先于记忆策略；没有策略且目录不同时询问。
单次 Current 使用活动目录，记忆 Current 使用本次启动目录或显式目录。
Session 使用选中记录的工作区；缺失目录且没有策略时沿用当前目录，明确选择 Session
却没有可用目录时报告错误。记忆保存 `tui.resume_cwd` 的策略值，不保存固定路径。
目录菜单默认 Session，Esc 选择 Session，Ctrl+C/Ctrl+D 退出；普通会话列表的取消语义独立。

## 阶段 1：历史查询与路径身份

- [x] 统一历史存储与列表的跨平台路径身份比较，覆盖 Windows verbatim、大小写和 `..`。
- [x] 指定会话 ID 的查询不受当前工作目录限制。
- [x] `--last` 保留默认工作区范围；`--all` 正确放宽范围。
- [x] 列表 Cwd/All 切换具有完整候选数据，`--all` 初始展示 All。
- [x] 定向测试、差异复核、更新本清单后提交并推送。

## 阶段 2：目录策略及显式启动目录

- [x] 增加严格校验的 `tui.resume_cwd = "session" | "current"`，未配置保持询问策略。
- [x] 增加进程级 `-C/--cd`，通过显式参数传递启动目录，不修改进程环境。
- [x] 区分不可变启动目录、活动工作目录和历史会话目录，删除 TUI 周期性 cwd 覆盖。
- [x] 配置解析上下文可随工作区准备和提交；目标信任判断使用目标目录。
- [x] 定向测试、差异复核、更新本清单后提交并推送。

## 阶段 3：完整工作区切换生命周期

- [x] 复用现有 Harness/Settings/Workspace owner，形成准备、提交、清理的明确边界。
- [x] 准备失败不改变旧会话；旧会话结束 Hook 使用旧目录。
- [x] 一致切换配置、权限、编码工具、子代理依赖、项目 Skills、MCP 和输入上下文。
- [x] 历史工作区只在目标目录生效后更新；后台资源和冻结中的 Run 不混用新旧上下文。
- [x] 覆盖跨项目切换、准备失败、清理失败、再次切换和关闭。
- [x] 定向测试及必要架构审计、更新本清单后提交并推送。

## 阶段 4：统一恢复交互

- [x] CLI 与 `/resume` 共享目录决策及恢复用例，接入四项目录菜单。
- [x] 保存记忆策略；保存失败展示错误并仍使用本次选择。
- [x] 成功后更新 Transcript、补全、Skills 和目录展示，失败不误换历史画面。
- [x] 覆盖当前目录、历史目录、两种记忆策略、显式目录、相同目录和缺失目录。
- [x] 覆盖取消、Esc、连续恢复的启动目录锚点及 Profile/CLI 覆盖。
- [x] 定向测试、差异复核、更新本清单后提交并推送。

## 阶段 5：收口复核

- [ ] 对照本清单及 Codex 参考逐项复查入口、状态所有者与失败路径。
- [ ] 同步稳定架构/操作文档与契约测试，删除被替代路径及临时兼容实现。
- [ ] 运行受影响测试、完整架构审计、compileall 和 `git diff --check`。
- [ ] 检查提交仅包含本任务文件，提交并推送通过复核的结果。

## 阶段 6：实施完成后的真机实测

- [ ] 所有实施阶段完成后，使用真实 Windows 终端/ConPTY 启动项目入口。
- [ ] 使用独立配置、状态根和两个测试工作区，验证跨目录查找及目录菜单。
- [ ] 验证四项选择、重启后的记忆、`--cd` 优先级、`--all` 和应用内再次恢复。
- [ ] 核验实际执行目录、项目配置与界面一致；不触碰用户真实历史及配置。
- [ ] 实测发现问题回到对应阶段修复、测试并推送，再继续实测直到完成。
- [ ] 清理独立实测资源，记录最终验证结果和提交。

## 校准记录

- 初始复核：118 项定向测试通过、2 项跳过。实际调用确认 `--all` 初始过滤、Windows
  路径身份、TUI cwd 覆盖和配置上下文未切换的问题。用户已有
  `CONTEXT_LEFT_ALIGNMENT_CHECKLIST.md` 不纳入本任务提交。
- 每阶段在此补充验证结果与必要调整；调整必须服务上述固定验收语义。
- 阶段 1：查询、选择器和 TUI 命令测试通过；目录比较在 SQLite 查询阶段执行，
  保证筛选先于 limit，已存储的合法路径拼写同样按统一身份比较，不增加字段别名。
- 阶段 1 复核：309 项定向检查通过。架构审计发现既有真实终端辅助入口
  `tests/pty/live_activity_trace.py` 按 `__file__` 深度推导根目录；在阶段 6 改为显式入口，
  与跨目录真机实测一并处理，其余此次涉及的架构检查已通过。
- 阶段 2：配置、CLI、启动上下文和命令回归合计 449 项通过、2 项跳过。
  `--cd` 限定本地 agent 运行入口，其他管理命令显式拒绝，避免悄悄忽略目录。
  配置支持先解析目标再提交工作区；完整信任交互和资源提交在阶段 3/4 接入。
- 阶段 3：Harness、配置、Skills 和组合根回归中 443 项通过、2 项跳过；另外三个
  文件补全测试因虚拟环境激活移除了工具 PATH 失败，恢复 rg 路径后该模块 24 项通过。
  目标信任与配置提交、输入上下文、模块归属补充检查 15 项通过。架构审计其余检查通过，
  仍保留前述真机辅助入口的既有根目录推导问题至阶段 6。共享恢复交互在阶段 4 接入。
- 阶段 4：CLI、应用内命令、目录菜单、输入与渲染回归 856 项通过。四项菜单保存
  策略值；单次与记忆 Current 分别使用活动目录和启动锚点。菜单新增明确的 Esc 结果
  及可选 Ctrl+D 中断语义，普通菜单保持原取消行为；选择当前会话保持草稿且不重复恢复。
