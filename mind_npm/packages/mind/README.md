# Mind

Mind npm command launcher.

Install with:

```bash
npm install -g @proxymind/mind
```

The package installs a `mind` command and loads the platform runtime from the
matching optional dependency.
