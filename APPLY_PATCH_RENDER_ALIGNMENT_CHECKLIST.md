# Apply patch 渲染对齐清单

目标：采用 Codex 的语法前景、增删标记和布局语义，支持完整的 IDE 无背景模式。
本清单跟踪本次分阶段实施；稳定架构以 `ARCHITECTURE.md` 为准。
对照来源：`codex-main/codex-rs/tui/src/diff_render.rs` 的差异布局、增删样式与有界高亮，
以及 `codex-main/codex-rs/tui/src/render/highlight.rs` 的 Mocha / Latte 和低色深主题。

## 不变量

- IDE 不生成背景色，输出边界继续阻止背景泄漏；禁背景不关闭语法或增删前景。
- 原始终端探测事实与渲染策略分离；未知色深按 ANSI 16 渲染，明确无色仍保持无色。
- `PatchView` 拥有结构化展示事实；前端不重新读取工作区文件，不改变工具或协议语义。
- 实时记录、历史重绘、审批预览与原生滚屏使用一致的样式、颜色和背景策略。
- 保留已有用户改动；每阶段完成定向验证和 diff 复核后独立提交、推送。

## 阶段一：颜色与背景策略

- [x] 统一未知色深的渲染降级规则，并保留原始能力事实。
- [x] 从冻结能力派生共享背景策略，patch 与 TUI 末端过滤器复用同一判断。
- [x] 删除 `rich` 对语法与背景的捆绑；无背景时显式选择增删正文前景。
- [x] 覆盖 IDE 明暗主题、未知色深、明确无色及最终终端字符样式。
- [x] 同步稳定文档，复核、提交并推送（`975fa0f9`；205 项定向测试通过）。

## 阶段二：共享语法主题与语言识别

- [x] 采用 Catppuccin Mocha / Latte 风格，细分配置键、变量、函数、类型、内建名称等角色。
- [x] 为 ANSI 16 提供明确语法角色映射；高亮不依赖背景能力。
- [x] Patch、Markdown、工具预览共享主题规则，移除固定暗色及被替代颜色常量。
- [x] 按文件名、扩展名及确实可用的首行 shebang 识别语言；未知语法保留 diff 前景。
- [x] 覆盖用户提供的 shell、gitignore、YAML 示例及有界失败路径（669 项定向测试通过，2 项跳过）。
- [x] 同步稳定文档，复核、提交并推送（`4d9f7c96`）。

## 阶段三：跨行状态与端到端收口

- [x] 分别解析 hunk 旧侧、新侧，避免删除和新增内容互相污染语法状态。
- [x] 保留上下文缺失的精确性限制、输入上限、行号、换行和重命名语义。
- [x] 复核实时记录、历史重绘、审批预览、缩放与原生滚屏的实际颜色和背景。
- [x] 执行受影响前端测试、编译检查和 `git diff --check`；架构审计执行但发现既有测试工具脚本路径违规，未改动无关文件。
- [ ] 完成清单复核，提交并推送，核对远端提交。

## 收口验证

- 前端回归共 2411 项通过、3 项跳过；其中 4 项文件补全测试在补齐测试进程的 ripgrep PATH 后补跑通过。
- 架构审计 137 项通过、1 项既有失败：`tests/pty/live_activity_trace.py:38` 从 `__file__` 层级推导仓库根。
  该写法在本次改动前的 `32875c10` 已存在，本次保留该无关脚本。
- 生产包编译检查和 `git diff --check` 通过。
- 颜色验证覆盖 5 种色深、IDE 明暗主题、原生 VT 字符样式、实时/历史/审批生命周期与 80→40→80 列重排。
